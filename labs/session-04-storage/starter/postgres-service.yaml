apiVersion: v1
kind: Service
metadata:
  name: postgres
  namespace: exercices
spec:
  # TODO: make this a headless service (set clusterIP to None)
  selector:
    app: postgres
  ports:
    - port: 5432
      targetPort: 5432
      protocol: TCP
