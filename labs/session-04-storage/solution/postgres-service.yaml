apiVersion: v1
kind: Service
metadata:
  name: postgres-tim  # Remplacez tim par votre prenom
  namespace: exercices
spec:
  clusterIP: None
  selector:
    app: postgres-tim
  ports:
    - port: 5432
      targetPort: 5432
      protocol: TCP
