apiVersion: apps/v1
kind: Deployment
metadata:
  name: api
  namespace: exercices
  labels:
    app: api
spec:
  replicas: 2
  selector:
    matchLabels:
      app: api
  template:
    metadata:
      labels:
        app: api
    spec:
      containers:
        - name: api
          image: europe-west9-docker.pkg.dev/cloud-447406/training/api:v1
          ports:
            - containerPort: 8080
          env:
            - name: ENVIRONMENT
              value: "training"
          # TODO: Add a livenessProbe
          # - httpGet on /health, port 8080
          # - initialDelaySeconds: 5
          # - periodSeconds: 10

          # TODO: Add a readinessProbe
          # - httpGet on /ready, port 8080
          # - initialDelaySeconds: 3
          # - periodSeconds: 5

          # TODO: Add a startupProbe
          # - httpGet on /health, port 8080
          # - failureThreshold: 30
          # - periodSeconds: 2

          # TODO: Add resource requests and limits
          # resources:
          #   requests:
          #     cpu: ???
          #     memory: ???
          #   limits:
          #     cpu: ???
          #     memory: ???
