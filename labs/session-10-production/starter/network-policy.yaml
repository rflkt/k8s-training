apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: api-allow-frontend
  namespace: exercices
spec:
  # TODO: Select pods this policy applies to
  # podSelector:
  #   matchLabels:
  #     app: ???

  # TODO: Define policy types (Ingress)
  # policyTypes:
  #   - ???

  # TODO: Allow ingress only from frontend pods on port 8080
  # ingress:
  #   - from:
  #       - podSelector:
  #           matchLabels:
  #             app: ???
  #     ports:
  #       - protocol: TCP
  #         port: ???
