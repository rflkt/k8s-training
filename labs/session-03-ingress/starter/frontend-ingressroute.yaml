apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: frontend-ingressroute
  namespace: exercices
spec:
  entryPoints:
    - web
  routes:
    # TODO: add a route with the following configuration
    # - match: Host(`frontend.training.local`)
    # - kind: Rule
    # - services: list with one entry pointing to the frontend service on port 80
