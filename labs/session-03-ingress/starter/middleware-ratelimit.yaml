apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  name: ratelimit-<NOM>  # Remplacez <NOM> par votre prenom
  namespace: exercices
spec:
  rateLimit:
    # TODO: set average to 100 (requests per second)
    # TODO: set burst to 200
