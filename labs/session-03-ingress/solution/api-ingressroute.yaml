apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: api-ingressroute
  namespace: exercices
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`api.training.test`)
      kind: Rule
      services:
        - name: api
          port: 80
