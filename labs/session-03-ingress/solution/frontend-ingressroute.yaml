apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: frontend-ingressroute
  namespace: exercices
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`frontend.training.test`)
      kind: Rule
      services:
        - name: frontend
          port: 80
