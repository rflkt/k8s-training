apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: frontend-ingressroute-tim  # Remplacez tim par votre prenom
  namespace: exercices
spec:
  entryPoints:
    - web
  routes:
    - match: Host(`frontend-tim.training.test`)
      kind: Rule
      services:
        - name: frontend-tim
          port: 80
