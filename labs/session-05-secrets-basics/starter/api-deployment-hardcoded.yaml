apiVersion: apps/v1
kind: Deployment
metadata:
  name: api
  namespace: exercices
spec:
  replicas: 1
  selector:
    matchLabels:
      app: api
  template:
    metadata:
      labels:
        app: api
    spec:
      containers:
        - name: api
          image: europe-west9-docker.pkg.dev/cloud-447406/training/api:v1
          ports:
            - containerPort: 8080
          env:
            - name: ENVIRONMENT
              value: "training"
            # WARNING: password hardcoded in plain text — never do this in production!
            - name: DB_PASSWORD
              value: "super-secret-password-123"
            - name: DB_HOST
              value: "postgres.exercices.svc.cluster.local"
            - name: DB_USER
              value: "api-user"
