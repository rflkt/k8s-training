apiVersion: secrets-store.csi.x-k8s.io/v1
kind: SecretProviderClass
metadata:
  name: gcp-secrets
  namespace: exercices
spec:
  # TODO: set the provider to "gcp"
  # TODO: add parameters section with:
  #   secrets: |
  #     - resourceName: "projects/<PROJECT_ID>/secrets/<SECRET_NAME>/versions/latest"
  #       path: "api-key"
  # HINT: the GCP secret name is "training-api-key"
