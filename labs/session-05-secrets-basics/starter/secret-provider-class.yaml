apiVersion: secrets-store.csi.x-k8s.io/v1
kind: SecretProviderClass
metadata:
  name: gcp-secrets-<NOM>  # Remplacez <NOM> par votre prenom
  namespace: exercices
spec:
  # TODO: set the provider to "gcp"
  # TODO: add parameters section with:
  #   secrets: |
  #     - resourceName: "projects/cloud-447406/secrets/training-api-key/versions/latest"
  #       path: "api-key"
  # HINT: the GCP secret name is "training-api-key" (already created in the GCP project)
