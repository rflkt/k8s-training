apiVersion: v1
kind: Secret
metadata:
  name: db-credentials
  namespace: exercices
# TODO: specify the type (Opaque)
# TODO: add a data section with the following keys:
#   - DB_PASSWORD: base64-encoded value of "super-secret-password-123"
#   - DB_USER: base64-encoded value of "api-user"
# HINT: use `echo -n "value" | base64` to encode
