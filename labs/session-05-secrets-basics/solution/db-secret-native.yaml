apiVersion: v1
kind: Secret
metadata:
  name: db-credentials-tim  # exemple : <NOM> = tim
  namespace: exercices
type: Opaque
data:
  # base64-encoded values (echo -n "value" | base64)
  # NOTE: base64 is encoding, NOT encryption — it can be decoded trivially
  DB_PASSWORD: c3VwZXItc2VjcmV0LXBhc3N3b3JkLTEyMw==  # super-secret-password-123
  DB_USER: YXBpLXVzZXI=                                  # api-user
